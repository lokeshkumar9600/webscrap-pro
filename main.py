from bs4 import BeautifulSoup
# soup = BeautifulSoup("file_name",'lxml')
# print(soup.prettify()) //formats the code
# grabbing the html tags
# tags = soup.find('h5') //only returns the first element
# print(tags)
# tags = soup.find_all('h5') //returns an array(list) of h5 tag

# course_cards = soup.find_all('div',class_="card") //finding by class
